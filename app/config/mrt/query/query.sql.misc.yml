queries:
  /queries/misc/now:
    sql: |
      select
        now() as current
  /queries/misc/admin:
    sql: |
      select id, ark, erc_what
      from inv.inv_objects
      where object_type='MRT-system'