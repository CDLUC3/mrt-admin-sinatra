source 'http://rubygems.org'
ruby '~> 3.2'

gem 'aws-sdk-ssm'
gem 'sinatra'
gem 'json'
gem 'rack'
gem 'rackup'
gem 'rack-contrib'
gem 'rubysl-base64'
gem 'octokit'
gem 'puma'

# These are the dependencies that are used only for unit tests.
group :test do
  gem "rspec"
  gem "rack-test"
end
